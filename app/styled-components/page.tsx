"use client";

import React from "react";
import styled from "styled-components";

const page = () => {
  return <Nav>STYLED-COMPONENTS</Nav>;
};

const Nav = styled.div`
  background-color: blueviolet;
`;

export default page;
