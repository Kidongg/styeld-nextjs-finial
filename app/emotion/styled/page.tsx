"use client";

import styled from "@emotion/styled";
import React from "react";

const page = () => {
  return <Nav>EMOTION-STYLED</Nav>;
};

const Nav = styled.div`
  background-color: blueviolet;
`;

export default page;
