"use client";

import { css } from "@emotion/css";
import React from "react";

const page = () => {
  return (
    <div
      className={css`
        background-color: blueviolet;
      `}
    >
      EMOTION-CSS
    </div>
  );
};

export default page;
